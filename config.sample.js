var config = {};

config.repos = [{
      name: 'GITHUB_ORG/GITHUB_REPO' //e.g. sualko/github-release-stats
   }
];

config.db = {
   host: 'localhost',
   user: '',
   password: '',
   database: 'github_release_stats'
};

module.exports = config;
